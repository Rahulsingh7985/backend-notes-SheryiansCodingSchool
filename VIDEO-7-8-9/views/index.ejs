<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body >
    <div class="main  p-10 w-full min-h-screen border-4 border-red-400 bg-orange-100">
        <div class="form  text-orange-800">
            <form action="/create" method="post">
                <input class="block mt-3 mx-5 py-2 bg-orange-200 gap-5  w-full rounded-lg outline-none text-center"
                    type="text" placeholder="TITLE GOES HERE" name="title">
                <textarea
                    class="block resize-none mt-3 mx-5 py-2 bg-orange-200 gap-5 w-full rounded-lg outline-none text-center"
                    placeholder="WRITE YOUE TASK HERE" name="details"></textarea>
                <input type="submit" class="mt-3  bg-orange-600 text-white rounded-md py-2 mx-5 p-10" value="create task">
            </form>
        </div>
        <div class="tasks flex">
            <% if(files.length>0){%>
                <% files.forEach(function(val){%>
                    <div class="task bg-orange-200 rounded-md w-72 p-10 mx-5 py-4 mt-10">
                        <h1 class="text-black font-bold tracker-tighter "><%= val %></h1>
                        <div class="flex justify-between w-full items-center mt-3">
                            <a class="text-blue-400 inline-block" href="/file/<%= val %>">Read more</a>
                            <a class="text-blue-400  " href="/edit/<%=val%>">edit</a>
                        </div>
                        
                    </div>
                    <% })%>
                        <%} else{%>
                            <h2 class="p-5 mt-10">No tasks yet...</h2>
                            <% }%>



        </div>
    </div>

</body>

</html>