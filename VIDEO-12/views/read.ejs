<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"> </script>
</head>

<body class="bg-zinc-900">
    <div class="main min-h-screen w-full h-full p-10 bg-zinc-900 text-white">
        <div class="nav mb-5">
            <a class="text-blue-500" href="/">Back to Home</a>
        </div>
        <h1 class="mb-5 font-bold text-2xl">Read Users</h1>
        <div class="users flex gap-3 flex-wrap">
            <% users.forEach(function(user){%>
                <div class="user max-w-42 p-3 bg-zinc-800 rounded-lg">
                    <div class="w-full h-[170px] bg-zinc-700 rounded-lg overflow-hidden">
                        <img class="w-full h-full object-cover object-top" src="<%= user.image%>">
                    </div>
                    <h3 class=" mt-3 tracking-tighter">
                        <%=user.name%>
                    </h3>
                    <h3 class="tracking-tighter text-zinc-500">
                        <%=user.email%>
                    </h3>
                    <div class="flex gap-10 mt-2">
                        <a class="text-zinc-500" href="/edit/<%=user._id%>">Edit user</a>
                        <a class="text-red-500" href="/delete/<%= user._id %>"
                            onclick="return confirm('Are you sure you want to delete this user?')">
                            Delete user
                        </a>
                    </div>
                </div>

                <% }) %>

        </div>
    </div>
</body>

</html>